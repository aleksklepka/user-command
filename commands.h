#include <iostream>
#include <cmath>
#include <stdlib.h>
#include <string>
#include <algorithm>
#include <cctype>

const int COMM_N = 4; // size of command list

int commIndex(std::string arr[], std::string ans); // returns the index of a command